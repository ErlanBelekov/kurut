# @kurut/cli is CLI interface for next-gen Kurut ORM

Available commands:

```
npx @kurut/cli auto-gen
npx @kurut/cli migrate new
npx @kurut/cli auto-gen
npx @kurut/cli migrate run
npx @kurut/cli db push
```
